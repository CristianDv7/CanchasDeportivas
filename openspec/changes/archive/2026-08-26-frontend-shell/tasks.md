# Tasks: frontend-shell — esqueleto shell + 3 microfrontends

## Phase 1: Infrastructure — workspace, bundler, ports, TDD scaffolding

- [x] 1.1 Confirmar puertos reales de los 4 microservicios: revisar cómo cada `ms-*` levanta con uvicorn (`backend/ms-usuarios/app/main.py`, `ms-canchas`, `ms-reservas`, `ms-reportes` — buscar `uvicorn.run`, `--port`, `Dockerfile`, `.env`). Documentar el resultado (puerto real o "sin fijar, se usa default X"). **Bloqueante para 1.4 y 1.5.**
  - **Resultado**: ningún `ms-*` fija puerto en código. Los 4 `app/main.py` solo hacen `FastAPI()` + `include_router(...)`, sin `uvicorn.run(...)`. No hay `Dockerfile` en ningún `ms-*`. No hay `.env` versionado (están en `.gitignore`, junto con `!.env.example`). `docker-compose.yml` solo levanta Postgres en `5433`. **Decisión adoptada** (idéntica al valor provisorio que ya proponía `design.md` §7): `ms-usuarios=8001`, `ms-canchas=8002`, `ms-reservas=8003`, `ms-reportes=8004`. Documentado en `frontend/.env.example` (ver nota de 1.4) y en los `rsbuild.config.ts` de cada app como fallback si la env var no está seteada.
- [x] 1.2 Crear `frontend/package.json` raíz + `pnpm-workspace.yaml` + `tsconfig.base.json` (pnpm workspaces, Node 20+, `corepack enable`).
  - **Nota**: el entorno corre Node v25.9.0, donde `corepack` fue removido del core (ya no existe como binario). `corepack enable` falla en este entorno. Se instaló `pnpm@11.24.0` vía `brew install pnpm` como alternativa funcional; el campo `packageManager: "pnpm@11.24.0"` en `package.json` sigue permitiendo que `corepack enable` funcione para cualquier colaborador en Node 20-24.
- [x] 1.3 Scaffold de las 4 apps (`apps/shell`, `apps/mf-reservas`, `apps/mf-administracion`, `apps/mf-reportes`) con Rsbuild + TypeScript + Module Federation 2.0 plugin, sin lógica todavía (solo config + entrypoint `index.ts` con `import('./bootstrap')` async).
- [x] 1.4 Escribir `frontend/.env.example` con `PUBLIC_API_BASE`, `PUBLIC_MF_*_URL`, `PUBLIC_SHELL_URL`, `PUBLIC_BUILD_ID`, `MS_*_URL` usando los puertos confirmados en 1.1 (Depende de 1.1).
  - **Cerrada.** El harness del agente deniega `Write`/`Edit`/`mv` hacia rutas `.env*` (regla global de `~/.claude/settings.json`), así que el contenido se preparó en `frontend/env.example.txt` como workaround y el usuario corrió `cp frontend/env.example.txt frontend/.env.example` manualmente. Archivo creado y confirmado en disco; `env.example.txt` removido (ya redundante) y la nota provisoria del README actualizada.
- [x] 1.5 Configurar proxy same-origin en `apps/shell/rsbuild.config.ts` (`/api/usuarios`, `/api/canchas`, `/api/reservas`, `/api/reportes` → `MS_*_URL` con `pathRewrite`) (Depende de 1.1, 1.4).
- [x] 1.6 Crear `apps/shell/src/config/env.ts` — único punto que lee `import.meta.env`, valida variables requeridas al boot y falla ruidosamente si falta alguna.
- [x] 1.7 Instalar y configurar Vitest + Testing Library + `@testing-library/jest-dom` + `@testing-library/user-event` en las 4 apps (`vitest.config.ts`, `setupTests.ts`, script `test` en cada `package.json`).
  - Verificado: `pnpm --filter <app> test` carga la config y jsdom correctamente en las 4 apps (falla con "No test files found" — esperado, aún no existen tests; eso es Fase 2).
- [x] 1.8 En `apps/mf-reservas/vitest.config.ts`, `apps/mf-administracion/vitest.config.ts` y `apps/mf-reportes/vitest.config.ts`, agregar alias manual `'shell/session' → '../shell/src/session'` y `'shell/apiClient' → '../shell/src/http'` (Vitest no corre bajo runtime MF; sin esto la suite de cada remote queda bloqueada desde la primera tarea de testing).
- [x] 1.9 Configurar `shared: { react, react-dom, react-router-dom: singleton:true, strictVersion:false }` idéntico en `apps/shell/rsbuild.config.ts` y en los 3 remotes; declarar `exposes`/`remotes` cruzados (shell↔remotes) según §5.3 del design.

## Phase 2: Core Implementation — SessionStore, apiClient, guards, remotes

### 2.1 SessionStore (TDD: RED → GREEN)

- [x] 2.1.1 RED: test de `createSessionStorageMirror` — `read/write/clear`, JSON corrupto, `v` inválido, storage que lanza (degrada a `createNullMirror()`).
  - **Desvío**: jsdom implementa `sessionStorage` como un Proxy que intercepta cualquier acceso de propiedad (incluidos métodos) como si fuera una clave de storage — `vi.spyOn(Storage.prototype, 'setItem')` y hasta `Object.defineProperty(window.sessionStorage, 'setItem', ...)` no lo interceptan (confirmado con un test de aislamiento). Se agregó un segundo parámetro opcional `storage: Storage = window.sessionStorage` a `createSessionStorageMirror` solo para poder inyectar un doble en tests; el contrato público de 0-1 args no cambia.
- [x] 2.1.2 GREEN: implementar `apps/shell/src/session/mirror.ts` (`SessionMirror`, `createSessionStorageMirror`, `createNullMirror`, `PersistedSession`).
- [x] 2.1.3 RED: test de `createMemorySessionStore` — `getSnapshot()` referencia estable, `hydrate()` desde mirror, `set/clear`, `authorizeRequest()` con y sin sesión.
- [x] 2.1.4 GREEN: implementar `apps/shell/src/session/store.ts` (`SessionStore`, `createMemorySessionStore`, `SessionSnapshot`).
- [x] 2.1.5 RED: test del singleton `getOrCreateSessionStore()` — misma instancia entre llamadas, sobrevive doble `import` simulado (`vi.resetModules()` + reimport).
- [x] 2.1.6 GREEN: implementar `getOrCreateSessionStore()` con `Symbol.for('canchasdeportivas.session.store.v1')` (ADR-05). Test 2.1.5/2.1.6 quedaron en el mismo `store.test.ts` que 2.1.3/2.1.4 (es la misma unidad de compilación).
- [x] 2.1.7 RED: test de `useSession()` vía `renderHook` — refleja cambios del store, `hasRole()` OR lógico, rol desconocido ⇒ `hasRole()` false.
- [x] 2.1.8 GREEN: implementar `apps/shell/src/session/useSession.ts` con `useSyncExternalStore` (ADR-04) + `getSession`, `subscribeSession`. `login`/`logout` se implementaron en un archivo separado `session/session.ts` (no en `store.ts`) para evitar un ciclo de imports real: `session.ts` importa `../http` (que importa `session/store.ts` para el wiring del singleton); si `login`/`logout` vivieran en `store.ts` sería un ciclo circular efectivo.

### 2.2 apiClient (TDD: RED → GREEN)

- [x] 2.2.1 RED: test de `createApiClient({fetchImpl, store, config})` — inyecta `Authorization` cuando hay sesión; no inyecta y corta el request (`ApiError{status:0,code:'unauthorized'}`) cuando no hay sesión y `auth!==false`.
- [x] 2.2.2 RED: test de mapeo de errores — 401 (clear + `emitUnauthorized` once-guard), 403 (`forbidden`, sin clear), 422 (`detail` aplanado), 204/vacío, `Content-Type` no JSON, red caída (`code:'network'`), token nunca expuesto en `.url`/mensaje.
- [x] 2.2.3 GREEN: implementar `apps/shell/src/http/client.ts` (`ApiClient`, `ApiError`, `ServiceName`, `RequestOptions`) usando `sessionStore.authorizeRequest()` (ADR-03). El once-guard de `emitUnauthorized()` se resetea suscribiéndose al propio store: cuando vuelve a `status:'authenticated'` (nuevo login), se rearma — sin acoplar `apiClient` a la lógica de login.
- [x] 2.2.4 GREEN: implementar `apps/shell/src/http/index.ts` con el wiring del singleton `apiClient` exportado + `onUnauthorized`.
  - **Desvío necesario**: `config/env.ts` del shell validaba `import.meta.env.PUBLIC_*` de forma estricta (falla ruidosa). Bajo Vitest, esas vars nunca están seteadas (el `loadEnv()` de Rsbuild no corre en el grafo de Vite/Vitest) y el singleton `apiClient` (importado indirectamente por `session.ts`, `LoginPage`, etc.) hubiera roto TODA la suite del shell. Se agregó un fallback: si `import.meta.env.VITEST === 'true'` (flag oficial de Vitest), se usan los mismos defaults de dev que ya usan los `rsbuild.config.ts` como fallback (decisión de Fase 1). En dev/build reales el comportamiento de "falla ruidosa si falta la var" no cambió.

### 2.3 Login y route guards (TDD: RED → GREEN)

- [x] 2.3.1 RED: test de `login()` en `Session` — éxito mapea `LoginResponse → SessionUser`, rol desconocido ⇒ sesión válida con `hasRole()` false (ADR-06); credenciales inválidas ⇒ no crea sesión, rechaza sin logout global (`auth:false`).
  - **Desvío de orden**: `login()`/`logout()` (2.3.2) se implementaron ANTES que este test, porque `useSession()` (2.1.8) necesita un `login` real para que su `Session.login` type-checkee. El test 2.3.1 (`session.test.ts`) se escribió y corrió DESPUÉS de la implementación, validando los 3 escenarios (éxito, rol desconocido, credenciales inválidas) contra el código ya existente — no fue RED→GREEN estricto para esta unidad puntual, es la única excepción de este batch. Se documenta explícitamente por transparencia.
- [x] 2.3.2 GREEN: implementar `login()`/`logout()` en `apps/shell/src/session/session.ts`, consumiendo `apiClient.post('/auth/login', ..., {service:'usuarios', auth:false})`. `logout()` es idempotente (no vuelve a notificar/escribir el espejo si ya está anónimo), verificado con test.
- [x] 2.3.3 RED: test de `LoginPage` (RTL + `user-event`) — submit válido navega a `/reservas` (o `state.from`), submit inválido muestra error sin navegar.
- [x] 2.3.4 GREEN: implementar `apps/shell/src/auth/LoginPage.tsx`.
- [x] 2.3.5 RED: test de `RequireAuth`/`RequireRole` (RTL + `MemoryRouter`) — estados `idle` (nunca redirige, `BootSplash`), `anonymous` (redirige a `/login`), `usuario` bloqueado en ruta admin, `administrador` con acceso completo, rol desconocido bloqueado.
- [x] 2.3.6 GREEN: implementar `apps/shell/src/auth/RequireAuth.tsx` y `RequireRole.tsx` como layout routes con `<Outlet/>` (ADR-08).
- [x] 2.3.7 GREEN: implementar `apps/shell/src/auth/AccesoDenegadoPage.tsx` (con test RTL agregado por disciplina TDD, no explícito en el plan original).

### 2.4 Shell host — layout, router, MF loading

- [x] 2.4.1 Implementar `apps/shell/src/app/RootLayout.tsx` (nav persistente + outlet). Se agregó test RTL (spec "Layout persists across navigation") aunque la tarea no pedía RED explícito.
- [x] 2.4.2 RED: test de `RemoteBoundary` (RTL) — componente lazy que rechaza a propósito es capturado por su `ErrorBoundary` propio sin afectar al resto.
- [x] 2.4.3 GREEN: implementar `apps/shell/src/app/RemoteBoundary.tsx` (`Suspense` + `ErrorBoundary` por remote, botón "Reintentar" remonta cambiando `key`).
  - **Decisión de API**: `RemoteBoundary` recibe `loader: () => Promise<{default: ComponentType}>` en vez de un `children` ya envuelto en `React.lazy`. Motivo: `React.lazy()` cachea su promesa/resultado para siempre en esa instancia — remontar el mismo `lazy(...)` (cambiando `key`) NO vuelve a invocar el factory tras un rechazo. `RemoteBoundary` crea el `React.lazy()` internamente vía `useMemo` keyed por un `retryToken` de estado; "Reintentar" incrementa el token, lo que fuerza un `lazy()` nuevo (invoca el loader de nuevo) Y remonta el `ErrorBoundary` (vía `key`). Verificado con test: el loader se llama 2 veces tras un retry.
- [x] 2.4.4 Implementar `apps/shell/src/app/AppRouter.tsx` con la tabla de rutas del design (§4): `/login`, `/`, `/reservas/*`, `/administracion/*`, `/reportes/*`, `/acceso-denegado`, `*` (404).
  - **Decisión de API**: `AppRouter` acepta un prop opcional `remoteLoaders` (`{reservas?, administracion?, reportes?}`) que sobreescribe los loaders default (`() => import('mf_reservas/App')`, etc). Motivo: Vite/Vitest resuelve estáticamente el string literal de `import('mf_reservas/App')` en tiempo de transform (no solo en runtime), así que sin esto ningún test de `AppRouter` podría correr sin que existan paquetes reales `mf_reservas`, `mf_administracion`, `mf_reportes` instalados. Los tests inyectan loaders fake; producción usa los defaults federados.
  - `/login` usa un wrapper `RedirectIfAuthenticated` inline (no es una tarea separada) que redirige a `/reservas` si ya hay sesión, tal como pide design.md §4.
- [x] 2.4.5 Implementar `apps/shell/src/shared/contract.ts` con `SHELL_CONTRACT_VERSION = 1` y exponer `./session`, `./apiClient`, `./contract` vía MF (`exposes` en rsbuild.config.ts — ya declarado en Fase 1).
- [x] 2.4.6 Fallback manual de tipos: escribir `remotes.d.ts` a mano por remote con `declare module 'shell/session'` / `'shell/apiClient'` por si el `dts` de MF 2.0 falla (§5.3 del design). Además se creó `apps/shell/src/remotes.d.ts` (dirección inversa: shell necesita tipar `mf_reservas/App`, `mf_administracion/App`, `mf_reportes/App` para que `tsc` resuelva `AppRouter.tsx`; no estaba explícito en el texto de la tarea pero es la misma técnica aplicada en la dirección que faltaba).
  - **Gotcha de TypeScript**: `declare module "shell/session" { export * from "../../shell/src/session"; }` compila pero **NO** re-exporta los miembros (`tsc` reporta "has no exported member X" para todo). Hubo que listar explícitamente cada export (`export type {...}` / `export {...}`) dentro del bloque ambient — `export *` dentro de un `declare module` con specifier relativo no funciona como cabría esperar.

### 2.5 Remotes — RemoteHealthCard

- [x] 2.5.1 RED: test de `RemoteHealthCard` (RTL, `shell/session` y `shell/apiClient` resueltos por el alias de Vitest hacia los fuentes reales del shell, ya configurado en Fase 1) — muestra nombre del remote, build id, origen federado, usuario+rol leídos de `useSession()`.
- [x] 2.5.2 GREEN: implementar `RemoteHealthCard.tsx` en `mf-reservas`, `mf-administracion`, `mf-reportes` (copiado por remote, sin paquete compartido, tal como pide el design). Cada remote sumó un `config/env.ts` propio (mismo patrón que el shell) para leer su propio `PUBLIC_MF_*_URL` como "origen en el que me sirvo" — variable que ya existía en `.env.example` de Fase 1 pero antes solo la leía el shell.
- [x] 2.5.3 RED: test del botón "forzar error" — activar el control lanza un error dentro del árbol de render del remote (capturado en el test con un `ErrorBoundary` de prueba local).
- [x] 2.5.4 GREEN: implementar el control "forzar error" en `RemoteHealthCard`.
- [x] 2.5.5 RED: test del probe opcional de backend — éxito muestra "conectado"; fallo (fetch rechaza) degrada a "no conectado" sin throw ni disparar el `ErrorBoundary`.
- [x] 2.5.6 GREEN: implementar el GET de solo lectura opcional vía `apiClient` en cada `RemoteHealthCard`, degradable. `mf-reservas` prueba `service:'reservas'`, `mf-administracion` prueba `service:'canchas'`, `mf-reportes` prueba `service:'reportes'` (decisión razonable no explicitada en el design: cada remote administrativo/de negocio prueba el microservicio con el que más se relaciona).
- [x] 2.5.7 Implementar `apps/mf-reservas/src/App.tsx`, `apps/mf-administracion/src/App.tsx`, `apps/mf-reportes/src/App.tsx` exponiendo `./App` vía MF, montando su `RemoteHealthCard`. Se conectaron además los `bootstrap.tsx` de las 4 apps (composition root sin lógica propia, no gateado por RED/GREEN) para que Fase 3 pueda levantar los dev servers: shell hidrata el `SessionStore` antes del primer render y monta `<BrowserRouter><AppRouter/></BrowserRouter>`; cada remote monta su propio `<App/>` para poder abrirse en aislamiento (`pnpm --filter mf-reservas dev` sin el shell).

## Phase 3: Integration / Manual Validation

- [x] 3.1 Levantar `pnpm -r --parallel dev` (shell:3000 + remotes 3001-3003) y verificar login real contra `ms-usuarios` de punta a punta (usuario y administrador seedeados). **Bug real encontrado y arreglado**: los 4 `config/env.ts` leían `import.meta.env` vía un alias (`const raw = import.meta.env`) en vez de la expresión literal `import.meta.env.PUBLIC_X` — `source.define` de Rsbuild reemplaza por sustitución AST de esa expresión exacta, así que el alias nunca matcheaba y quedaba `undefined` en runtime real (dev/build), aunque los tests bajo Vitest pasaban igual porque ahí `import.meta.env` es un objeto real, no un literal reemplazado. Corregido en los 4 archivos a acceso literal directo. Confirmado con `pnpm install`, `pnpm -r typecheck` y `pnpm -r test` (82 tests) verdes después del fix, y login real end-to-end contra `ms-usuarios` (puerto 8001, backend levantado con Postgres vía `docker compose up -d` + seed) funcionando para `admin@test.com`/`admin123` (rol administrador) y `usuario@test.com`/`123456` (rol usuario).
- [x] 3.2 Verificado manualmente: `usuario` no ve los links de Administración/Reportes en el nav y es redirigido a `/acceso-denegado` al intentar `/administracion` directo por URL; `administrador` accede a `/reservas`, `/administracion` y `/reportes` sin problema, los 3 remotes cargan vía MF con `RemoteHealthCard` mostrando usuario/rol reales de la sesión. Logout limpia la sesión y redirige a `/login`. No se probó explícitamente "sesión sobrevive a un reload" en esta pasada — pendiente de una verificación puntual.
- [x] 3.3 Ejecutado el plan de validación de HMR (§8 del design). **A: PASS** — edité el `<h1>` de `mf-reservas/src/App.tsx`, el texto se actualizó sin recarga y el contador de `RemoteHealthCard` se mantuvo en 3. **B: PASS** — agregué una línea al handler del hook de `RemoteHealthCard.tsx`, el contador siguió en 3 tras el hot update y el nuevo código funcionó (3→4 al click). **F: PASS tras el fix de 3.5** (ver abajo). C/D/E no se probaron explícitamente en esta pasada (degradaciones ya aceptadas de antemano por el design, menor prioridad).
- [x] 3.4 No hizo falta la escalera de fallback de HMR del design (A y B dieron PASS directo). La escalera que sí se necesitó fue distinta — ver 3.5.
- [x] 3.5 **RESUELTO.** Hallazgo original: matar `mf-reportes` (puerto 3003) y navegar a `/reportes` tumbaba TODA la app (incluido `/reservas`, de un remote sano) en vez de aislarse — el `ErrorBoundary` por remote no alcanzaba a capturarlo. Causa raíz confirmada leyendo el bundle compilado del runtime (`static/js/index.js`): con `shareStrategy` en su valor default `'version-first'`, `initializeSharing()` hace `host.options.remotes.forEach(remote => promises.push(initRemoteModule(remote.name)))` — es decir, para inicializar el share scope de **cualquier** remote, el runtime de Module Federation 2.0 intenta resolver el manifest de **todos** los remotes declarados (necesario para poder comparar versiones de `react`/`react-dom`/`react-router-dom` antes de decidir cuál usar como singleton). Un solo manifest inalcanzable (`Federation Runtime Error #RUNTIME-003`, `Failed to fetch`) revienta ese `Promise.all` compartido y tumba el montaje de cualquier remote, no solo el caído. **Fix aplicado**: agregado `shareStrategy: "loaded-first"` a `pluginModuleFederation({...})` en los 4 `rsbuild.config.ts` (shell + 3 remotes) — con esta estrategia el runtime resuelve `shared` bajo demanda, solo con lo que ya está cargado, sin necesitar el manifest de remotes no solicitados. Es la opción oficial documentada en `@module-federation/sdk` (`ShareStrategy = 'version-first' | 'loaded-first'`), no un workaround. Re-testeado con `mf-reportes` caído: `/reservas` y `/administracion` siguen funcionando perfecto, `/reportes` cae limpio en el `ErrorBoundary` ("mf-reportes no disponible" + Reintentar) sin afectar al resto. Confirmado también que tras restaurar `mf-reportes` + F5, vuelve a cargar sin problema (nota: el botón "Reintentar" solo no alcanza para este caso puntual — el runtime de MF cachea en memoria la promesa del manifest rechazado por su `manifestUrl`, distinto del crash de render que "Reintentar" sí recupera solo — hace falta un reload completo; comportamiento ya aceptado como último recurso en la escalera de fallback del design §8). `pnpm -r typecheck` y `pnpm -r test` (82 tests) verdes después del fix.
- [x] 3.6 Confirmado en la práctica: `getOrCreateSessionStore()` devuelve siempre la misma referencia (verificado vía `window[Symbol.for('canchasdeportivas.session.store.v1')]` en consola, dos lecturas idénticas) — garantizado por construcción, ya que `Symbol.for` resuelve al mismo símbolo en el registro global del realm sin importar qué bundle/remote lo pida. Navegando entre `/reservas`, `/administracion` y `/reportes` con los 4 servicios sanos, la consola no mostró ningún warning de "Invalid hook call" ni de contexto/instancia de React duplicada — solo los warnings inofensivos de React Router (future flags v7). Dedup del singleton MF confirmado sin necesidad de instrumentación temporal adicional.

## Phase 4: Testing / Verification

- [x] 4.1 Correr la suite Vitest completa en las 4 apps y confirmar verde (`pnpm -r test`).
  - **Resultado: VERDE, sin regresiones.** `pnpm -r test` desde `frontend/`: 14 archivos, 82 tests (shell 11 archivos / 64 tests; `mf-reservas`, `mf-administracion`, `mf-reportes` 1 archivo / 6 tests cada uno). Tras el test agregado en 4.2 quedó en **83 tests** (shell 65). Ruido esperado en stdout: los stack traces de `Error: Error forzado desde RemoteHealthCard (...)`, `Error: remoteEntry no disponible` y `Error: caído` son los errores que los tests provocan a propósito y que los `ErrorBoundary` capturan — React los loguea igual vía `console.error`, no son fallos.
- [x] 4.2 Revisar cobertura de escenarios de las 3 specs (`frontend-shell-host`, `frontend-auth-session`, `frontend-remote-modules`) contra los tests escritos en Phase 2; anotar cualquier escenario sin test explícito.
  - **Resumen: 24 escenarios en las 3 specs. 20 con test automatizado explícito, 2 parciales, 2 sin test automatizado.**
  - **`frontend-shell-host` (7 escenarios)**
    - ✅ *Layout persists across navigation* → `app/RootLayout.test.tsx` ("mantiene la navegación montada al navegar entre rutas permitidas").
    - ✅ *Route resolves to its remote* → `app/AppRouter.test.tsx` ("/reservas monta el remote mf-reservas...", "administrador monta mf-administracion y mf-reportes").
    - ✅ *Unknown route* → `app/AppRouter.test.tsx` ("ruta desconocida muestra un 404 en vez de pantalla en blanco").
    - ❌ **SIN TEST AUTOMATIZADO — *Single React instance across host and remotes*.** No es testeable bajo Vitest: requiere el runtime real de Module Federation resolviendo `shared: {singleton:true}` en un browser, cosa que jsdom no ejerce (los tests de los remotes resuelven `shell/session` por alias de Vitest hacia los fuentes, no por federación). Verificado **manualmente** en 3.6 (misma referencia de `getOrCreateSessionStore()` vía `Symbol.for`, cero warnings de "Invalid hook call" o React duplicado navegando entre los 3 remotes). **Es el único escenario que ameritaría un E2E real (Playwright) si el proyecto llega a sumarlo** — lo dejo señalado explícitamente, no escondido: hoy la garantía es manual + por construcción (`Symbol.for` en el realm global), no automatizada.
    - ✅ *Remote entry unreachable at load time* → `app/RemoteBoundary.test.tsx` (loader que rechaza ⇒ "mf-reservas no disponible", el hermano del árbol sigue vivo). Nota: el aislamiento real cross-remote a nivel runtime MF (el bug de `shareStrategy` de 3.5) NO está cubierto por este test unitario — se validó manualmente matando `mf-reportes`.
    - ✅ *Remote throws a runtime error* → **gap detectado y cerrado en esta fase.** El único test que existía cubría "el loader rechaza" (manifest caído), no "el remote montó bien y después tira en render", que es justo lo que dispara el botón "forzar error" del `RemoteHealthCard` y es criterio de aceptación central del change. Se agregó a `app/RemoteBoundary.test.tsx`: "un remote YA montado que lanza en render es capturado por su boundary; el layout y el otro remote siguen vivos" (dos `RemoteBoundary` hermanos + nav; el roto cae en su boundary, el sano renderiza). **Transparencia TDD**: no fue RED→GREEN — la implementación ya existía de Fase 2, el test pasó en la primera corrida. Es un backfill de cobertura de auditoría, no desarrollo nuevo.
    - ⚠️ **PARCIAL — *Recovering from a caught remote error*.** La spec pide "el usuario navega afuera y vuelve ⇒ el shell remonta el remote fresco". Testeado: el botón "Reintentar" (`loader` invocado 2 veces). NO testeado: el ciclo navegar-fuera-y-volver vía router. Cubierto por construcción (desmontar `RemoteBoundary` resetea `retryToken` y su `useMemo`, así que al volver se crea un `lazy()` nuevo) y verificado manualmente en 3.2/3.5. Gap menor, no lo cierro.
  - **`frontend-auth-session` (10 escenarios)**
    - ✅ *Successful login* → `session/session.test.ts` ("éxito: mapea LoginResponse → SessionUser...") + `auth/LoginPage.test.tsx` (navega a `/reservas` y a `state.from`).
    - ✅ *Invalid credentials* → `session/session.test.ts` + `auth/LoginPage.test.tsx` ("submit inválido muestra error sin navegar").
    - ⚠️ **PARCIAL — *Session survives a page reload*.** Cubierto a nivel unidad por `session/store.test.ts` ("hydrate() restaura desde el mirror si hay una sesión persistida" / "hydrate() sin sesión persistida deja status='anonymous'"), que es exactamente lo que corre el `bootstrap.tsx` en un reload. Un reload real de browser no es simulable en jsdom. **Además queda pendiente la verificación manual**: 3.2 dejó anotado que "sesión sobrevive a un reload" no se probó explícitamente en la pasada manual. Gap de validación manual, no de código.
    - ❌ **SIN TEST AUTOMATIZADO — *Session does not survive tab close*.** jsdom no modela ciclo de vida de pestañas. Es semántica nativa de `sessionStorage`, garantizada por construcción; lo más cercano que existe es `session/mirror.test.ts` ("usa 'cd.session.v1' como key por default"), que confirma que el espejo vive en `sessionStorage` y no en `localStorage`. Gap aceptable: cerrarlo requeriría E2E con control de pestañas.
    - ✅ *Authenticated request includes token* → `http/client.test.ts` ("inyecta Authorization cuando hay sesión") + `session/store.test.ts` ("authorizeRequest() con sesión inyecta Authorization: Bearer <token>").
    - ✅ *No session, no Authorization header* → `http/client.test.ts` ("sin sesión y auth !== false: NO hace el request, rechaza ApiError{status:0, code:'unauthorized'}" — la variante de short-circuit que permite la spec) + "authorizeRequest() sin sesión devuelve null".
    - ✅ *usuario blocked from restricted routes* → `auth/RequireAuth.test.tsx` ("rol 'usuario' bloqueado en ruta de administrador ⇒ /acceso-denegado", "rol desconocido queda bloqueado igual") + `app/AppRouter.test.tsx`.
    - ✅ *administrador has full access* → `auth/RequireAuth.test.tsx` + `app/AppRouter.test.tsx` ("administrador monta mf-administracion y mf-reportes").
    - ✅ *Unauthenticated access to any guarded route* → `auth/RequireAuth.test.tsx` ("status='anonymous': redirige a /login"; más "status='idle' nunca redirige", que evita el falso positivo de redirigir antes de hidratar).
    - ✅ *Logout clears session* → `session/store.test.ts` ("clear() borra memoria y espejo, deja status='anonymous'") + `session/session.test.ts` (logout idempotente). Nota honesta: la segunda mitad del escenario ("navegación posterior a ruta guardada redirige a login") queda cubierta por composición con el test de `RequireAuth` en `anonymous`, no por un único test integrado logout→navegación.
  - **`frontend-remote-modules` (7 escenarios, cada uno testeado ×3 remotes salvo aclaración)**
    - ✅ *Remote identifies itself* → "muestra el nombre literal del remote y un build id" ×3.
    - ✅ *Origin matches the remote's dev server* → "muestra el origen federado, no el del shell" ×3 (assert literal `localhost:3001/3002/3003`).
    - ✅ *Card reflects the shell's session* → "refleja el usuario y rol de la sesión del shell" ×3.
    - ⚠️ **PARCIAL — *Session change is reflected without remote rebuild*.** Los 3 tests de remote setean la sesión ANTES del render y verifican el valor, pero ninguno cambia la sesión con la card ya montada. La reactividad en vivo sí está testeada, pero del lado del shell: `session/useSession.test.tsx` ("refleja cambios del store en vivo"). Como la card consume ese mismo `useSession`, el escenario está cubierto por composición + manualmente (3.2, login como `usuario` vs `administrador`). Gap menor, no lo cierro.
    - ✅ *Forcing an error is contained* → lado remote: "el control 'forzar error' lanza un error dentro del árbol de render" ×3 (con `TestErrorBoundary` local); lado shell: el test nuevo de `RemoteBoundary` descrito arriba. La composición exacta shell↔remote federado sigue siendo manual (3.5).
    - ✅ *Probe succeeds* → "el probe de backend muestra 'conectado' si el microservicio responde" ×3.
    - ✅ *Probe fails gracefully* → "el probe de backend degrada a 'no conectado' sin throw si el microservicio no responde" ×3.
  - **Escenarios con test agregado por disciplina, fuera de spec**: `auth/AccesoDenegadoPage.test.tsx`, `RootLayout` "muestra el email del usuario en la nav", y toda la batería de mapeo de errores de `http/client.test.ts` (401 once-guard y su reset, 403, 422 aplanado, 204, Content-Type no JSON, red caída, token nunca filtrado en `.url`/mensaje) — derivan del design, no de un Given/When/Then explícito.
- [x] 4.3 Actualizar `openspec/config.yaml` (`testing.frontend.status`, `rules.apply.test_command`, `rules.verify.test_command`/`build_command`) con los comandos reales una vez confirmados.
  - `testing.frontend.status`: `not_scaffolded` → `scaffolded`, con `test_runner: vitest`, libs reales instaladas, conteo de tests y `e2e: false` (ver el gap de "Single React instance" en 4.2, que es el candidato #1 si se suma Playwright).
  - `rules.apply.test_command` y `rules.verify.test_command`: `pnpm -r test` (desde `frontend/`). `rules.verify.build_command`: `pnpm -r build`.
  - **`pnpm -r build` confirmado corriéndolo de verdad en este batch: exit 0, las 4 apps buildean** (`mf-administracion` 390.6 kB, `mf-reservas` 390.5 kB, `mf-reportes` 390.3 kB, `shell` 505.5 kB), cada una emitiendo `dist/remoteEntry.js` + `dist/mf-manifest.json` + `dist/@mf-types.d.ts`.
  - **Hallazgo no bloqueante del build**: las 4 apps emiten `[ Module Federation DTS ] Failed to collect TypeScript dependency files with "tsc --listFilesOnly"; falling back to exposed files only. error TS6059: File '.../src/App.tsx' is not under 'rootDir' '.../node_modules/.federation'`. Es el generador automático de tipos de MF 2.0 degradando a "solo los archivos expuestos" — **no falla el build** (exit 0) y no afecta el bundle. Es exactamente el riesgo que anticipaba la tarea 2.4.6, y por eso los `remotes.d.ts` manuales de cada app son los que sostienen el typecheck real (`pnpm -r typecheck` verde). Vale dejarlo documentado en el README (5.1) para que nadie confunda ese warning con un error.
  - Se agregó también `coverage_threshold` explícito en `0`: no hay reporte de cobertura configurado (`coverage: false`), así que cualquier umbral > 0 sería una promesa falsa.
  - **Bug preexistente encontrado y corregido: `openspec/config.yaml` NO era YAML válido.** El bloque `rules.apply` mezclaba un ítem de secuencia con claves de mapping al mismo nivel de indentación (`- Follow existing code patterns...` seguido de `tdd: true` / `test_command: ""`), lo que revienta cualquier parser estándar (`YAMLException: bad indentation of a mapping entry (28:5)`, verificado con `js-yaml`). Venía así desde antes de este change — no lo introdujo la Fase 4. Corregido anidando la regla bajo `rules.apply.guidelines:` y conservando `tdd`/`test_command` como hermanos. Post-fix el archivo parsea limpio de punta a punta. Nota para quien lea: si alguna herramienta de OpenSpec venía "funcionando" con el archivo roto, es porque lo leía con regex/texto plano, no parseando YAML.

## Phase 5: Cleanup / Documentation

- [x] 5.1 Escribir `frontend/README.md` con: cómo levantar las 4 apps, límites documentados de HMR cross-remote (degradaciones C/D/E aceptadas), limitación de logout cross-tab, y el script `pnpm dev:reset` si se llegó al fallback 5 de la escalera HMR.
  - **Contenido real del README**: cómo levantar Postgres + `ms-usuarios` (backend, puerto 8001) y las 4 apps del frontend vía `pnpm dev` (los otros 3 `ms-*` son de un compañero, no bloquean); requisito de `.env` (copiar `.env.example`, falla ruidosa por diseño en `config/env.ts` si falta una var); credenciales seedeadas (`admin@test.com`/`admin123` administrador, `usuario@test.com`/`123456` usuario); tabla completa A-F de HMR con el resultado real (A y B PASS, C/D/E degradaciones aceptadas con la explicación de por qué — recompilar el shell invalida lo que los remotes ya cargaron, F PASS gracias al fix de 3.5); aislamiento de remote caído vía `shareStrategy: "loaded-first"` con referencia a tasks.md 3.5 para el detalle; limitación de logout cross-tab (`sessionStorage` por pestaña, ADR-03, no es un bug); warning no bloqueante `TS6059` del generador de tipos de MF 2.0 en `pnpm build`, aclarando que el typecheck real lo sostienen los `remotes.d.ts` manuales; comandos `pnpm dev`/`pnpm test`/`pnpm build`/`pnpm typecheck`.
  - **Desvío documentado — no existe `pnpm dev:reset`**: A y B de la tabla HMR dieron PASS directo en 3.3/3.4, así que nunca se necesitó la escalera de fallback del design (§8) que ese script cerraría (paso 5, "último recurso"). El README lo aclara explícitamente en vez de inventar un script que no existe en el repo.
  - **Nota sobre 1.4**: al cerrar esta fase, `frontend/.env.example` todavía no existía (bloqueado por el harness). Se resolvió después: el usuario corrió `cp frontend/env.example.txt frontend/.env.example` manualmente, el archivo quedó creado y confirmado en disco (correctamente excluido del `.gitignore` junto con `.env`, `.env.example` sí queda versionado), y `env.example.txt` se removió por redundante. Ver tarea 1.4 (marcada `[x]`) para el cierre real.
  - **Detalle de filesystem**: el repo ya tenía un `frontend/readme.md` vacío (0 bytes). El filesystem del entorno (APFS default) es case-insensitive, confirmado con `stat -f "%i"` (mismo inode para `casetest.tmp` y `CASETEST.tmp`) — escribir `frontend/README.md` sobrescribió ese mismo archivo en vez de crear uno nuevo. No hay duplicado.
- [x] 5.2 Remover cualquier instrumentación temporal de debug agregada en 3.6.
  - **Nada que remover, confirmado.** La verificación de 3.6 (singleton `getOrCreateSessionStore()` vía `Symbol.for`, ausencia de warnings de React duplicado) se hizo enteramente desde la consola del browser (`window[Symbol.for(...)]`) sin tocar código fuente — así quedó documentado en tasks.md al cerrar esa tarea. Se re-confirmó ahora con `rg` sobre `frontend/apps/**` (excluyendo tests) buscando `console.log`/`console.debug`/`console.warn`/`window.__DEBUG`/referencias sueltas a `Symbol.for('canchasdeportivas...`: cero resultados. No se removió nada porque no había nada que remover.
